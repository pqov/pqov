name: Test OV16_160_64_pkc_skc

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: ${{ matrix.cc }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cc:
          - gcc
          - clang
    steps:
    - uses: actions/checkout@v3
    - name: test
      run: make PARAM=1 VARIANT=3 test
      env:
        CC: ${{ matrix.cc }}
    - name: sanitizers
      run: make SANITIZERS=1 PARAM=1 VARIANT=3 test
      env:
        CC: ${{ matrix.cc }}
