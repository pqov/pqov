name: Test OV16_160_64_pkc

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: ${{ matrix.cc }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        cc:
          - gcc
          - clang
    steps:
    - uses: actions/checkout@v3
    - name: test
      run: make PARAM=1 VARIANT=2 test
      env:
        CC: ${{ matrix.cc }}
    - name: asan
      run: |
        make clean
        make ASAN=1 PARAM=1 VARIANT=2 test
      env:
        CC: ${{ matrix.cc }}
    - name: ubsan
      run: |
        make clean
        make UBSAN=1 PARAM=1 VARIANT=2 test
      env:
        CC: ${{ matrix.cc }}
    - name: memsan
      run: |
        make clean
        make MEMSAN=1 PARAM=1 VARIANT=2 test
      env:
        CC: ${{ matrix.cc }}
